# URL del backend real (server-side, no expuesta al cliente)
# Por defecto usa el devtunnel de desarrollo
BACKEND_URL=https://lqbjk5bh-8081.use2.devtunnels.ms/query

# URL base del servidor de medios (MinIO, S3, etc.)
NEXT_PUBLIC_MEDIA_BASE_URL=http://localhost:9000
